# Implementation Plans

Generated by the `improve` skill on 2026-06-24, against commit `2abd6f3`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, run every
verification command, and update your row in the table when done.

Repo: `github-scanner` ("GitHub Healthcheck") — Cloudflare Workers + Hono
backend (`src/`), React/Vite SPA (`web/`), D1 database, cron alerts. Package
manager **pnpm**. Verification commands every plan uses:

| Purpose | Command |
|---------|---------|
| Typecheck | `pnpm typecheck` |
| Unit tests | `pnpm test` |
| Integration tests (Workers pool + D1) | `pnpm test:integration` |
| Build SPA (+ prerender) | `pnpm build:web` |
| Worker bundle dry-run | `pnpm exec wrangler deploy --dry-run` |

Never deploy locally (CI on push to `master` deploys). Never edit an existing
migration — add a new numbered file. Keep `src/engine/` pure (no I/O).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix failed-email clone-alert suppression | P1 | S | — | DONE (wt agent-ad2c…, 62081ca) |
| 002 | Unit tests for `auth/crypto.ts` | P1 | S | — | DONE (wt agent-a493…, dc5b931) |
| 003 | Add `CLAUDE.md` | P1 | S | — | DONE (wt agent-a9fa…, 7ec89fd) |
| 004 | Add Biome lint + format + CI gate | P1 | M | — | DONE (wt agent-a854…, c81be55; revised to 6-file diff) |
| 005 | Make email `page()` escape-by-default | P2 | S | — | DONE (wt agent-a3b2…, a9b166f) |
| 006 | Isolate per-repo scan failures | P2 | S | 011 (recommended) | DONE (wt agent-a38c…; +3rd call site, behavior-neutral) |
| 007 | Bound cron subscriber concurrency | P2 | M | 012 (recommended) | DONE (wt agent-a518…, 8a88f67) |
| 008 | Code-split the admin dashboard | P2 | S | — | DONE (wt agent-aa9c…) |
| 009 | Origin-check CSRF defense-in-depth | P2 | S | — | DONE (wt agent-a39b…, b66d95b; +custom Origin mw, see note) |
| 010 | Add missing D1 indexes | P3 | S | — | DONE (wt agent-a652…) |
| 011 | Characterization tests: GitHub data layer | P2 | M | — | DONE (wt agent-ab5d…, 29 tests) |
| 012 | Tests: session lifecycle + OAuth state checks | P2 | M | — | DONE (wt agent-a9dd…, 7 tests incl. happy path) |
| 013 | Enable `noUncheckedIndexedAccess` in `web/` | P3 | S | 004 (recommended) | DONE (wt agent-ad0d…, 0 errors surfaced) |
| 014 | Clear dev-dependency advisories | P3 | S | — | BLOCKED (deferred — dev-only; vitest4+pool-workers0.16 breaks test:integration w/ ChaiStyleAssertions; vitest3 path trades for new undici/ws highs. Retry when pool-workers ships vitest-4 compat) |
| 015 | [spike] Public scan API | P3 | M | — | DONE (wt agent-a141…, design doc 356L) |
| 016 | [spike] Report→takedown draft | P3 | M | — | DONE (wt agent-a753…, doc + pure buildTakedownDraft +14 tests) |
| 017 | [spike] Watch any repo for alerts | P3 | M | — | DONE (wt agent-afb1…, doc + pure normalizeWatchTargets +17 tests) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Execution run 2026-06-24

All 17 plans were dispatched to isolated-worktree executors (sonnet) and reviewed
by the advisor. **16 APPROVED** → each committed to branch `advisor/NNN-<slug>`
off `2abd6f3`; **1 deferred** (014). Branches are NOT merged or pushed — merging is
the maintainer's decision. Notable approved deviations: 006 also guarded a 3rd
(single-repo) call site (behavior-neutral); 009 added a custom Origin middleware
because `hono/csrf` only guards form content-types; 004 was revised down from a
37-file import-reorder churn to a 6-file diff.

**Reconcile + merge 2026-06-24:** the 16 approved branches were assembled (by an
executor, reviewed by the advisor) onto **`improve/integration-2026-06-24`**
(commit `ad0d807`, 30 files, +3025/−26). Only `src/alerts/cron.ts` (004+007) was
multi-touched; git auto-merged it cleanly with both changes intact. Full suite
re-verified green on the integrated branch: typecheck, lint, 219 unit + 38
integration tests, build (prerender intact), wrangler dry-run.

**Merged + shipped 2026-06-24:** opened as PR
[#1](https://github.com/CopyJosh/github-healthcheck/pull/1), CI `build-test`
passed, merged into `master` as commit `2fa7ae8`. The push-to-master Deploy
workflow then ran green — applied D1 migration `0003_admin_indexes.sql` and
deployed the Worker to Cloudflare. Integration branch + all `advisor/*` branches
and their worktrees were cleaned up. **014 remains deferred** (BLOCKED above);
spikes 015–017 landed as docs + unwired pure functions awaiting a build
decision.

## Dependency notes

- **Tests before risky refactors.** 011 and 012 are characterization tests for
  the GitHub data layer and the auth/cron lifecycle. They should land before
  the refactors that touch those areas: 007 (cron concurrency) ideally after
  012; 006 (scan failure isolation) ideally after 011. Each refactor plan also
  ships its own focused test, so they are not hard-blocked — but landing the
  characterization layer first is the safer order.
- **001 is self-contained** — it ships its own cron-diff regression test; it
  does not need 012 first, though 012 deepens cron coverage afterward.
- **013 after 004** — enabling the stricter compiler flag is easier to review
  once the linter (004) is in place, and both touch the gate set.
- **The three spikes (015–017) are design/investigation plans**, not
  build-everything plans. Their deliverable is a short design doc + open
  questions (and optionally a thin prototype), for the maintainer to approve
  before any production build.

## Findings considered and rejected (do not re-audit)

- **`.dev.vars.example` missing `ALERT_FROM_EMAIL`** — not a bug. It is set in
  `wrangler.jsonc` `vars`, which `wrangler dev` applies locally; `.dev.vars`
  only *overrides*. Local email has a valid `from`.
- **Snapshot's extra serial `getCommitFiles` call** (`github/snapshot.ts:122`)
  — bounded by the 4-wide worker pool and the per-scan repo cap; not an N+1.
- **Login case-normalization** — GitHub logins are case-canonical and
  `isBootstrapAdmin` already lowercases for comparison, so the role/admin and
  suspension paths are robust. Near-zero practical impact; not worth a migration.
- **`pnpm audit` moderate/low advisories** — dev-tree noise floor; the 1
  critical + 7 high are addressed (with caveats) in plan 014.

## Lower-leverage findings NOT turned into standalone plans

Recorded so they aren't lost; fold into adjacent work or a future pass:

- Admin list panels (`AdminUsers/Inbox/Scans/Reports`) have a stale-response
  race on rapid filter/search changes (no in-flight guard). Admin-only (single
  user = owner), so low user impact. Fix: an `ignore` flag in each effect cleanup.
- `getSession` returns the session-id **hash** as `session.id` (latent; no
  readers today). Either drop `id` from `SessionData` or rename to `idHash`.
- Two divergent "dedupe by suspect" impls (`clone-detection.ts:96` keeps
  highest-confidence; `cron.ts:79` keeps first-seen) + duplicated
  "select top-N source repos" logic across `scan.ts`/`alerts.ts`/`cron.ts`.
  Consolidate when next touching the clone path (overlaps 001/006/011).
- Rate-limit `retryAfterSec` can over-estimate after a burst overshoot
  (`ratelimit/store.ts:42` uses `MIN(created_at)`).
- CI re-installs deps + rebuilds the SPA in the `deploy` job
  (`deploy.yml`) — pass `web/dist` as an artifact, or merge the jobs.
- Unstructured Worker logging (8 `console.log` string sites, no request IDs).
- Stale comment in `vitest.config.ts:3-4` ("integration tests can be added
  later" — they exist) and README "npm test" wording.
